# 기여 가이드

## 제출 기준

- 하나의 Pull Request에는 하나의 스킬 또는 밀접한 개선만 포함합니다.
- 스킬 이름은 소문자, 숫자, 하이픈만 사용하고 64자 미만으로 작성합니다.
- `SKILL.md`의 `description`에는 스킬의 역할과 사용 시점을 분명히 적습니다.
- 모든 배포 스킬에는 `SKILL.md`와 내용이 대응하는 한글 번역본 `SKILL.ko.md`를 함께 제출하고, 변경 시 두 파일을 같은 Pull Request에서 갱신합니다.
- 사용자 권한, 배포 대상, 기술 스택을 임의로 확대하지 않습니다.
- 스크립트를 추가했다면 실행 방법과 검증 가능한 결과를 Pull Request에 적습니다.

## 검토 기준

리뷰어는 스킬이 재사용 가능한지, 요청 범위를 보존하는지, 비밀정보나 특정 개인의 권한을 전제하지 않는지, 그리고 자동 검사를 통과하는지를 확인합니다.

오류 수정은 재현 방법과 기대·실제 결과를 포함해 제출합니다.
